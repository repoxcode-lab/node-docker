/**
 * Docker container change prop
 */

export interface DockerContainerChangeProp {
  id : string;
}

/**
 * Docker container change response
 */

export interface DockerContainerChangeResponse {
  Path : string;
  Kind : number;
}
